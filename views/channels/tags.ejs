<h2>Live updates for #<%= tag %></h2>
<p class="help">Updates will appear here as soon as they are tagged on Instagram.</p>
<ul class="append-live-data" data-subscription="channel:tags:<%= tag %>">
<% for(var i=0; i<media.length; i++) { %>
  <li><a href="<%= media[i].link %>"><img src="<%= media[i].images.standard_resolution.url %>" /></a></li>
<% } %>
</ul>